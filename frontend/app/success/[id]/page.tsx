"use client";
import {useEffect,useState} from "react";import {useRouter} from "next/navigation";import PaytmShell from "@/components/PaytmShell";
export default function Success(){const [payment,setPayment]=useState<any>();const router=useRouter();useEffect(()=>{const x=sessionStorage.getItem("payment");if(x)setPayment(JSON.parse(x))},[]);return <PaytmShell nav={false}><section className="panel" style={{textAlign:"center"}}><div className="successmark">✓</div><div className="eyebrow">Payment successful</div><h1 className="title">Your order is confirmed</h1><p className="muted">{payment?.merchant?.name||"Sweet Crumbs"} has received ₹{payment?.amount||750} and will have your order ready by 6:30 PM.</p><div className="understood" style={{textAlign:"left"}}><b>Eggless chocolate cake</b><div className="meta"><span>1 kg</span><span>Tomorrow</span><span>Pickup</span></div><small className="muted">Intent → match → offer → Paytm payment. Closed loop.</small></div><button className="primary" onClick={()=>router.push("/ask")}>Done</button></section></PaytmShell>}

